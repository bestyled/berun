---
name: Presets
---

# Presets

Presets are a root set of runners that accomplish a well-defined task or workflow. For example,
a preset may create a well-defined React environment using Webpack.

Programmatically, they are no different than other runenrs, but are identified separately
as they generally are included in dependencies of other runners, but rather are the root set
that contain multiple fluent adapters and runners in order to 

## Included Presets

* React, using Webpack as build packager
* React, using Fuse-Box as build packager
* BeStatic for documentation